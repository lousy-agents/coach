{
    "$schema": "https://docs.renovatebot.com/renovate-schema.json",
    "extends": [
        "config:recommended",
        "helpers:pinGitHubActionDigestsToSemver",
        "security:openssf-scorecard"
    ],
    "automerge": true,
    "platformAutomerge": true,
    "lockFileMaintenance": {
        "enabled": true,
        "automergeType": "branch"
    },
    "semanticCommitType": "fix",
    "semanticCommitScope": "deps",
    "pinDigests": true,
    "dependencyDashboard": true,
    "postUpdateOptions": [
        "gomodTidy",
        "gomodUpdateImportPaths"
    ],
    "packageRules": [
        {
            "description": "Disable native mise manager due to untrusted config errors",
            "matchManagers": ["mise"],
            "enabled": false
        },
        {
            "description": "Pin all npm dependencies to exact versions",
            "matchManagers": ["npm"],
            "rangeStrategy": "pin"
        },
        {
            "description": "Group Onsi test packages",
            "matchManagers": ["gomod"],
            "matchPackageNames": [
                "github.com/onsi/ginkgo/v2",
                "github.com/onsi/gomega"
            ],
            "groupName": "onsi-test"
        },
        {
            "description": "Group golang.org/x modules",
            "matchManagers": ["gomod"],
            "groupName": "golang-x",
            "matchPackageNames": [
                "/^golang\\.org/x//"
            ]
        },
        {
            "description": "Bump mise.toml min_version and every CI jdx/mise-action version together",
            "matchPackageNames": ["jdx/mise"],
            "groupName": "jdx/mise"
        }
    ],
    "customManagers": [
        {
            "description": "Track Go version in mise.toml (custom manager because the native mise manager is disabled above)",
            "customType": "regex",
            "managerFilePatterns": [
                "/(^|/)mise\\.toml$/"
            ],
            "matchStrings": [
                "(?:^|\\n)go\\s*=\\s*\"(?<currentValue>[^\"]+)\""
            ],
            "depNameTemplate": "golang/go",
            "datasourceTemplate": "github-tags",
            "extractVersionTemplate": "^go(?<version>.*)$"
        },
        {
            "description": "Track Node.js version in mise.toml (custom manager because the native mise manager is disabled above)",
            "customType": "regex",
            "managerFilePatterns": [
                "/(^|/)mise\\.toml$/"
            ],
            "matchStrings": [
                "(?:^|\\n)node\\s*=\\s*\"(?<currentValue>[^\"]+)\""
            ],
            "depNameTemplate": "nodejs/node",
            "datasourceTemplate": "github-tags",
            "versioningTemplate": "node"
        },
        {
            "description": "Track mise minimum version in mise.toml (custom manager because the native mise manager is disabled above)",
            "customType": "regex",
            "managerFilePatterns": [
                "/(^|/)mise\\.toml$/"
            ],
            "matchStrings": [
                "min_version\\s*=\\s*\"(?<currentValue>[^\"]+)\""
            ],
            "depNameTemplate": "jdx/mise",
            "datasourceTemplate": "github-releases"
        },
        {
            "description": "Track jdx/mise-action version pin in ci.yml so it bumps with mise.toml min_version",
            "customType": "regex",
            "managerFilePatterns": [
                "/(^|/)\\.github/workflows/ci\\.yml$/"
            ],
            "matchStrings": [
                "uses: jdx/mise-action[^\\n]+\\n\\s+with:\\n\\s+version:\\s*\"(?<currentValue>[^\"]+)\""
            ],
            "depNameTemplate": "jdx/mise",
            "datasourceTemplate": "github-releases"
        }
    ]
}
