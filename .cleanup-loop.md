# Cleanup Loop State

## Pass 2

### Note

Pass 1 removed this file from git tracking during a later correctness-review
pass on this same PR, reasoning it was agent-process bookkeeping that didn't
belong in the repo. That contradicted this loop's own explicit instruction
("keep it tracked ... it ships with the PR; the reviewer deletes it at
merge") -- the removal was made without the loop prompt's literal text in
context. Restored tracking at the start of this pass; `.gitignore`'s
`.cleanup-loop.md` entry (added in the same correctness-review pass) is
reverted in the same commit.

Baseline for this pass matches pass 1's recorded baseline (gofmt/go-vet
clean, pkg/codesignal 184/184, cmd/coach suite green). In-scope files
unchanged from pass 1's list (merge-base with origin/main is still
`62e38fab92eb0b332830bc8d2a9d063fb1e5ab1b`).

### Report — Pass 2

```
tests    0 renamed, 0 split, 0 added, 0 deleted
comments 282 → 285   (in-scope files only; before = start of this pass)
structure 4 changes
suite    pass (matches baseline)    lint pass (matches baseline)
status   PASS 2/5
commits  f1e822f chore: restore cleanup-loop state file tracking
commits  2d18516 refactor(cli): extract expectIncompleteVerdictDiscrimination helper
commits  7a4bd8f test(codesignal): collapse the 8 TestGolden_* functions into one table test
commits  8df9a5b test(cli): merge the disabled-schema-1 Describe into the Go backend suite
commits  22b291a test(cli): fold the two internal-looking-import negative controls into a table

kept comments (non-obvious only)
cmd/coach/no_findings_verdict_acceptance_test.go (expectIncompleteVerdictDiscrimination doc)  shared-assertion contract, new this pass

questions
Pass 1's TS-sidecar three-When-block DescribeTable candidate was investigated and declined: fixture content, presence of installFakeTSSidecar, and presence/content of the message assertion are entangled rather than orthogonal, and the hung case's 60s real-wall-clock cost is better kept visible as its own When than hidden in a table row. No action needed; not a ruling.
Pass 1's golden_test.go "diagnosticsInput() mismatched.go comment" question was investigated and found to rest on a false premise: no such comment exists or ever existed in the file (confirmed via git log -p across the whole repo). The underlying mismatched.go fixture behavior was independently verified correct against current production logic (pkg/codesignal/codesignal.go's validateFileChange). No action needed; not a ruling.
project_go_backend_acceptance_test.go's 5-test diff-mode lifecycle/Changed-flag group (3 lifecycle cases + 2 Changed-flag cases, all sharing run/decode/assert scaffolding) was considered for a DescribeTable and declined: each case's setup differs enough that a table would need a per-entry setup closure plus an optional-Changed-check plus an optional-summary-assertion closure, which trades each test's current prose-readability for indirection without a clear net win. Recommend leaving as five explicit Whens; a human could reasonably weigh this differently.
Whether buildDisabledProjectAnalysisReportPair's byte-identical-report invariant should extend to the TS backend's disabled-analysis path (currently only exercised for Go) is an open coverage question; recommend a human product call, not a mechanical fix.
Whether the frozen-schema golden test (JSON only) should extend to freeze --format=text rendering too is a real coverage gap the existing #269-dependency note only partially covers; recommend a human product call given it needs new golden fixtures.
Several Expect failure messages carry issue-number narrative (e.g. "issue #259"); recommend a human style call on whether that belongs in test failure messages the way AGENTS.md forbids it in code comments.
The "keeps location-aware diagnostic order when appending a project diagnostic" spec in project_contract_acceptance_test.go is a whitebox test of withProjectDiagnostic's sort behavior sitting inside a CLI-level acceptance file; recommend a human call on whether it belongs in pkg/codesignal instead.
```

## Pass 1

### Baseline (pass-1, permanent reference)

- `mise run gofmt`: exit 0, no files listed.
- `mise run go-vet` (`go vet ./...`): exit 0.
- `go test ./pkg/codesignal/... -run TestCodeSignalAcceptance -v -count=1`: PASS, 184/184 specs, 0 failed.
- `go test ./cmd/coach/... -run TestCoachAcceptance -count=1`: PASS (234.443s).
- No linter beyond `gofmt`/`go vet` is configured in this repo (`acceptance-style-check` is a structural guard, not a style linter; no golangci-lint config found).
- Baseline failing tests: **none**. Baseline lint violations: **none**.

### In-scope files (PR #309 vs `origin/main` merge-base `62e38fab92eb0b332830bc8d2a9d063fb1e5ab1b`)

1. `AGENTS.md` — prose documentation, not source code. Comment/SOLID/test rules target code; scoping decision: excluded from mechanical comment/SOLID rules as inapplicable (not a "pure data/schema/config file" ruling-required case — it's plainly prose, no judgment call needed), included in scope only for the mechanical "PR touches it" bookkeeping.
2. `README.md` — same as above, prose documentation.
3. `cmd/coach/no_findings_verdict_acceptance_test.go` — Ginkgo acceptance test (T6, mostly new in this PR: ~250 changed lines).
4. `cmd/coach/project_contract_acceptance_test.go` — Ginkgo acceptance test (T3 addition: 43 new lines in a pre-existing file).
5. `cmd/coach/project_go_backend_acceptance_test.go` — Ginkgo acceptance test, **entire file in scope** (816 lines total; T2 changed 146 of them). Large pre-existing file — applying "practical bounds": full comment/structure audit on the changed region plus any comment that fails the bar wherever found; broader pre-existing restructuring only where clearly warranted, not a line-by-line rewrite of untouched, already-clean code.
6. `cmd/coach/project_ts_backend_acceptance_test.go` — Ginkgo acceptance test, entire file in scope (297 lines; T4/T5 changed 124 of them).
7. `cmd/coach/testdata/fake_ts_sidecar/main.go` — hand-written Go fixture (not vendored/generated), entire file in scope (322 lines; this PR changed 50 of them across T4/T5).
8. `pkg/codesignal/golden_test.go` — stdlib-style Go test (not Ginkgo — matches the file's pre-existing, established idiom per AGENTS.md's own carve-out), entire file in scope (T7 added 147 of its lines).
9. `pkg/codesignal/project_boundary_language_parity_acceptance_test.go` — Ginkgo acceptance test, entirely new in this PR (388 lines, T1).

### Pending rulings

(none — every item below is a deferred judgment call within this pass's
"high-signal subset" allowance, not a blocking Ruling-required escalation)

### Note on scope

An earlier, differently-scoped cleanup pass already ran on this branch
before this pass's baseline was taken (commits `a7b05f0`..`4d6b0ff`,
covering `pkg/codesignal/report.go`, `coverage_types.go`,
`internal/codesignalcli/analyze.go`, `cmd/coach/main.go` — production
files outside PR #309's own diff). That pass's report is preserved in
this file's prior revision; it is superseded by this one, which is
scoped correctly to the 9 files PR #309 actually touches. This pass's
baseline measurements were taken after that earlier work had already
landed, so its comment/test counts are independent of it.

### Report — Pass 1

```
tests    5 renamed, 1 split, 0 added, 0 deleted
comments 343 → 272   (in-scope files only; before = start of this pass)
structure 4 changes
suite    pass (matches baseline)    lint pass (matches baseline)
status   PASS 1/5
commits  3a38c62 refactor(codesignal): rename opaque helpers and dedup MachineEvidence key extraction
commits  5581037 docs(codesignal): delete comments restating code or already-carried-by-structure facts
commits  ff76131 refactor(codesignal): use reflect.StructField.IsExported in the frozen-name walker
commits  4ffbef4 docs(codesignal): delete comments restating adjacent code in golden_test.go
commits  adb4c03 test(codesignal): split TestFrozenSchema_FieldNames into named subtests
commits  129c2ef refactor(cli): name project_contract_acceptance_test's base/head revision vars by role
commits  8761db7 test(cli): name project-contract specs by their actual failure condition
commits  1dc7f18 docs(cli): trim project-contract acceptance comments to non-obvious facts
commits  479de2f docs(cli): trim redundant comments from Go backend project acceptance specs
commits  635f2c1 test(cli): unify qualified-verdict spec names across TS sidecar failure modes
commits  fdfe667 docs(cli): trim duplicated comments in the fake TS sidecar fixture
commits  8b7986f docs(cli): delete restated helper comments in the no-findings verdict spec
commits  d62e7fc test(cli): fold discrimination-rationale comments into their Expect messages

kept comments (non-obvious only)
pkg/codesignal/project_boundary_language_parity_acceptance_test.go:150  Go/TS evidence-key asymmetry
pkg/codesignal/golden_test.go (assertMatchesGolden doc)                JSON field-order lock rationale
pkg/codesignal/golden_test.go (reflectJSONFieldNames doc)              reflection-walk contract
pkg/codesignal/golden_test.go (TestFrozenSchema_FieldNames doc)        issue #269 dependency note
cmd/coach/project_contract_acceptance_test.go (comment B)              loadProjectConfig ordering invariant
cmd/coach/project_contract_acceptance_test.go (comment D)              diagnostic re-sort key order
cmd/coach/project_contract_acceptance_test.go (comment C)               backend-registration limitation
cmd/coach/project_go_backend_acceptance_test.go (handlersImportingUnresolved) classifyGoImport discriminating-test trap
cmd/coach/project_go_backend_acceptance_test.go (severity-order It body) sort.SliceStable false-green trap
cmd/coach/project_go_backend_acceptance_test.go (catch-all "." prefix)  matchLayer silent-no-op trap
cmd/coach/project_go_backend_acceptance_test.go (dotless-module test)   previously-silent bug context
cmd/coach/project_ts_backend_acceptance_test.go (wall-time block)       60s do-not-shorten trap
cmd/coach/project_ts_backend_acceptance_test.go (BeforeSuite comment)   Ginkgo single-BeforeSuite limitation
cmd/coach/testdata/fake_ts_sidecar/main.go (hang-marker branch)         Go deadlock-detection gotcha
cmd/coach/no_findings_verdict_acceptance_test.go (paddingBytes doc)     30s-timeout do-not-shrink trap
cmd/coach/no_findings_verdict_acceptance_test.go (both suppression comments) cross-file real-vs-absent-finding proof

questions
golden_test.go's 14 inline group-label comments inside frozenReportJSONFieldNames's map literal were left as-is; recommend keeping them (they label JSON schema groups a table-driven rewrite would still need) rather than treating as a violation.
golden_test.go's diagnosticsInput() mismatched.go edge case comment was not re-verified against current diagnostic-kind logic this pass; recommend a follow-up pass confirm it still reflects reality before next edit to that helper.
Collapsing golden_test.go's 8 TestGolden_* functions into one DescribeTable-style table test is a real structural improvement but touches every one of their per-case assertions; recommend a dedicated follow-up pass, not this one.
project_go_backend_acceptance_test.go has two top-level Describe blocks ("real Go project-language backend" and "disabled legacy schema-1 compatibility") that share fixtures via buildDisabledProjectAnalysisReportPair; recommend evaluating a merge in a follow-up pass once the DescribeTable question above is also settled, so both restructurings land together.
Whether buildDisabledProjectAnalysisReportPair's byte-identical-report invariant should extend to the TS backend's disabled-analysis path (currently only exercised for Go) is an open coverage question; recommend a human product call, not a mechanical fix.
Several Expect failure messages carry issue-number narrative (e.g. "issue #259"); recommend keeping issue references only where the assertion documents a regression, per AGENTS.md's PR-narrative-does-not-belong-in-code rule cutting the other way for comments but not necessarily for test failure messages -- recommend a human style call.
Whether the frozen-schema golden test (JSON only) should extend to freeze --format=text rendering too is a real coverage gap the existing #269-dependency note only partially covers; recommend a human product call given it needs new golden fixtures.
project_ts_backend_acceptance_test.go's three sidecar-failure When blocks (absent/crashed/hung) are near-identical shape and a strong DescribeTable candidate; recommend a dedicated follow-up pass, not folded in here since diff blast radius is not small.
The "keeps location-aware diagnostic order when appending a project diagnostic" spec in project_contract_acceptance_test.go is a whitebox test of withProjectDiagnostic's sort behavior sitting inside a CLI-level acceptance file; recommend a human call on whether it belongs in pkg/codesignal instead.
no_findings_verdict_acceptance_test.go's two "suppresses a real finding, not an absent one" comments cross-reference specs in other files by prose description rather than by name; recommend a follow-up pass convert these to doc comments on the referenced It blocks themselves, so the cross-reference survives an unrelated rename.
The two "renders different verdict text" Its in no_findings_verdict_acceptance_test.go duplicate the same clean-vs-incomplete comparison structure once for TS and once for Go; recommend evaluating a shared helper in a follow-up pass once the DescribeTable/Describe-merge questions above are also decided, so related restructuring lands together.
```
