# Cleanup Loop State

## Pass 1 (PR #345)

### Note on prior content

This file has repeatedly carried Pass 1 state for whichever PR was still
open at the time, because each PR's own cleanup-loop run finds a stale,
already-merged predecessor's record here and supersedes it rather than
resuming its pass count (that predecessor's state-file lifecycle rule --
"it ships with the PR; the reviewer deletes it at merge" -- was never
honored at merge time, for #309 and then again for #346). This run
follows the same convention: #309 (`claude/awesome-lamport-naceva`,
merged via `2a36a6f`) and #346 (the `ux-advocate` agent, issue #319,
merged to `main` while this PR was still open) are both closed and
superseded; their content no longer serves any purpose and is not
carried forward. This PR's own Pass 1 record, below, is unchanged from
before this merge -- PR #346 never touched any file this PR touches, so
there is nothing to reconcile beyond this file itself.

### Baseline (pass-1, permanent reference, PR #345)

- `mise run opencode-plugin-test`: 28/28 pass.
- `mise run gofmt`: exit 0, no files listed.
- `mise run go-vet` (`go vet ./...`): exit 0.
- No linter targets Markdown in this repo (no `.markdownlint*` config, no
  `remark`/`prettier` task in `mise.toml`). The two checks above are the
  only project-wide checks and are unaffected by this PR's files.
- Baseline failing tests: **none**. Baseline lint violations: **none**.

### In-scope files (PR #345 vs `origin/main` merge-base `ba0762a22937746b659dec24be0e7b9517114e29`)

1. `.agents/skills/designing-for-intent/SKILL.md` — a Claude Skill
   definition: YAML frontmatter plus prose procedure and one YAML example
   schema block. Not application source code: no functions, no types, no
   test suite, no code-comment markers (`#`/`//`/`<!-- -->`) anywhere in
   the file — confirmed by full read and by grep (every `#` match is a
   Markdown heading). SOLID/structure rules target code design and do not
   apply to a single prose document; the "comments" rule targets
   non-executable annotations on code, and none exist here to prune. The
   document's prose *is* its content, not a comment on adjacent code.
2. `.claude/skills/designing-for-intent` — a symlink to file 1. No content
   of its own.

### PR intent (from PR body, restated from the tests)

None.

### Report — Pass 1

```
tests    0 renamed, 0 split, 0 added, 0 deleted
comments 0 → 0   (in-scope files only; before = start of this pass)
structure 0 changes
suite    pass (matches baseline)    lint pass (matches baseline)
status   DONE — thorough
commits  (none — no violations found; nothing to change)

kept comments (non-obvious only)
(none — zero code-comment markers exist in either in-scope file)

questions
The pre-existing multi-pass cleanup history for PR #309 sitting in `main` (superseded above) indicates that PR's reviewer did not delete this file at merge as its own convention requires; recommend a human note this as a one-off process gap -- now recurring (see #346) -- worth a durable fix (e.g. a merge-time check or a documented manual step) rather than each PR re-discovering and re-superseding it.
```

---

## PR #347 — Pass 1

### Note

This file already existed on this branch, tracking PR #309 (merged into
`main` before this branch was rebuilt fresh from `origin/main` for the
current PR #347). Its original Pass 1/Pass 2 sections belonged entirely
to that earlier, unrelated, already-merged PR -- different diff,
different in-scope files, different baseline. This pass started as a
new "PR #347" section at Pass 1, scoped to PR #347's own merge-base and
changed files, intending to leave the stale PR #309 content in place
below as a preserved record.

Merging `origin/main` afterward surfaced a conflict here: PR #346 (a
sibling PR built off the same `ba0762a` merge-base, merged into `main`
first) ran this same loop independently, reached the same "PR #309's
content is stale and unrelated" conclusion, and resolved it by deleting
that content rather than preserving it. Since that precedent is now the
live state of `main`, this merge resolution follows it rather than
resurrecting what PR #346 already removed -- the PR #309 content is
dropped here too, and this section sits alongside PR #346's rather than
above stale history.

### Baseline (pass-1, permanent reference for PR #347)

- `mise run gofmt`: exit 0, no files listed.
- `mise run go-vet`: exit 0.
- `go test ./internal/codesignalcli/... -run Acceptance -count=1`: PASS.
- `go test ./internal/validationtasks/... -run Acceptance -count=1`: PASS (37/37 at pass start).
- `mise run js-ci`: PASS (60/60 js-test, plus ts-analyzer-embed-stale-check).
- No linter beyond `gofmt`/`go vet` is configured for Go; no eslint config exists for `js/semantics`.
- Baseline failing tests: **none**. Baseline lint violations: **none**.

### In-scope files (PR #347 vs `origin/main` merge-base `ba0762a22937746b659dec24be0e7b9517114e29`)

74 files changed total. 65 are `internal/codesignalcli/tsanalyzerasset/**` -- a
checked-in, machine-generated copy of a `js/semantics` build output, excluded
from comment/structure/test rules per "Skip ... fully generated artifacts."
`js/semantics/package-lock.json` (npm-generated lockfile) is excluded on the
same basis. `js/semantics/package.json` and `renovate.json` are pure
JSON config with no code and (for package.json) no comments; excluded from
comment/SOLID rules as inapplicable, same treatment PR #309's precedent (see
PR #346's note above) gave `AGENTS.md`/`README.md` -- not a Ruling-required
case, just plainly not code. Remaining in-scope files for the mechanical
rules:

1. `internal/codesignalcli/ts_analyzer_asset.go` -- new production Go.
2. `internal/codesignalcli/ts_analyzer_asset_acceptance_test.go` -- new Ginkgo acceptance test.
3. `internal/validationtasks/node_version_policy_acceptance_test.go` -- new Ginkgo acceptance test.
4. `internal/validationtasks/ts_analyzer_embed_stale_check_wiring_acceptance_test.go` -- new Ginkgo acceptance test.
5. `js/semantics/scripts/sync-ts-analyzer-embed.mjs` -- new hand-written Node script.
6. `mise.toml` -- pre-existing config file; comment bar applies to its new comments, structural/SOLID rules don't meaningfully apply (no functions/types).

### Pending rulings

None. Every judgment call this pass (mise.toml's two race/blind-spot
comments, the embed.FS-mode-quirk comment, the MaterializeTSAnalyzer
scope-boundary paragraph) was resolved by applying the two-gate test
directly -- none needed escalation.

### Report — Pass 1

```
tests    0 renamed, 0 split, 5 added, 0 deleted
comments 82 → 60   (in-scope files only; before = start of this pass)
structure 0 changes
suite    pass (matches baseline)    lint pass (matches baseline)
status   DONE — thorough
commits  ef8b0f1 test(validationtasks): pin engines.node parsing behavior directly
commits  ed0eeaa docs(codesignalcli): delete comments the code or tests already carry

kept comments (non-obvious only)
internal/codesignalcli/ts_analyzer_asset.go:15  embed/sync/drift-CI contract
internal/codesignalcli/ts_analyzer_asset.go:31  embed.FS mode-0444 quirk
internal/codesignalcli/ts_analyzer_asset.go:49  exported digest godoc
internal/codesignalcli/ts_analyzer_asset.go:84  test-seam rationale
internal/codesignalcli/ts_analyzer_asset.go:91  error/cleanup contract + scope note
mise.toml:115  js-test/sidecar-build race guard
mise.toml:140  intent-to-add blind-spot guard
js/semantics/scripts/sync-ts-analyzer-embed.mjs:3  build-order prerequisite trap
internal/validationtasks/ts_analyzer_embed_stale_check_wiring_acceptance_test.go:20  untestable cross-file CI fact

questions
(none)
```

## Pass 1 (PR #348)

### Note on prior content

At the time this pass started, this file carried Pass 1/2 records for a
different, already-merged PR (#309), left behind in violation of this
loop's own delete-at-merge contract. That content (baseline/report for
PR #309's 9 files: `golden_test.go`, `no_findings_verdict_acceptance_test.go`,
`project_go_backend_acceptance_test.go`, etc.) had zero relevance to this
PR (#348, issue #324), which touches a disjoint set of files, so this pass
started fresh rather than continuing "pass N+1" against the wrong PR's
state. (PR #309's own content is preserved in this file's git history at
commit `bd2d729` if it is ever needed again.) A second PR (#346) hit the
identical situation independently and resolved it the same way, merging
into main as the "Pass 1 (PR #346)" section above while this pass was
still in flight on this branch; both sections are kept side by side here
rather than one overwriting the other, since they describe disjoint PRs.

### Baseline (pass-1, permanent reference for PR #348)

- `mise run gofmt`: exit 0, no files listed.
- `mise run go-vet` (`go vet ./...`): exit 0.
- `go test ./internal/codesignalcli/... -count=1`: PASS, all subtests green (10.8s).
- `go test ./cmd/coach/... -run TestCoachAcceptance -count=1`: PASS (237.8s).
- No linter beyond `gofmt`/`go vet` is configured in this repo (`acceptance-style-check` is a structural guard, not a style linter; no golangci-lint config found).
- Baseline failing tests: **none**. Baseline lint violations: **none**.

### In-scope files (PR #348 vs `origin/main` merge-base `ba0762a22937746b659dec24be0e7b9517114e29`)

1. `cmd/coach/main.go` -- entire file in scope (621 lines total; this PR added ~90 of them: the `--check-project` flag, `validateCheckProjectFlags`, `runCheckProject`). Large, pre-existing, shared CLI entry point serving several other codesignal modes untouched by this PR. Practical bounds applied: full comment/structure audit on the added region; the untouched majority (other flags' dispatch, `--suggest-project-config`, etc.) was scanned for narrative-citation violations (none found) but not restructured, since that logic predates and is unrelated to this PR's concern.
2. `cmd/coach/project_readiness_acceptance_test.go` -- entirely new in this PR (Ginkgo acceptance specs for `--check-project`). Full audit.
3. `internal/codesignalcli/project.go` -- entire file in scope (532 lines total; this PR added the `maxProjectConfigRoots` bound, the `gitOperationalBoundErrorKind` type, and `parseProjectConfig`/`loadProjectConfigForReadiness`, and renamed/extended `validateProjectConfigJSON`'s body). Practical bounds applied: full audit on the added/touched region; `parseProjectConfig`'s pre-existing validation body (roots/layers/forbidden-imports guard clauses, ~90 lines, substantively unchanged by this PR beyond the rename and one 3-line insertion) was left unextracted -- see questions below.
4. `internal/codesignalcli/project_readiness.go` -- entirely new in this PR. Full audit.
5. `internal/codesignalcli/project_readiness_render.go` -- entirely new in this PR. Full audit; already clean, no changes needed.
6. `internal/codesignalcli/project_readiness_test.go` -- entirely new in this PR. Full audit.

### PR intent (from PR body, restated from the tests)

PR body: adds a read-only TypeScript project-readiness contract and a
new `coach codesignal --baseline --check-project --project-language
typescript` CLI mode. From the tests alone:
`project_readiness_acceptance_test.go`'s top-level Describe ("coach
codesignal --baseline --check-project --project-language typescript")
and its nested Whens/Its document exactly this -- a read-only,
versioned readiness verdict (status/checks/gaps/next_actions) for a
committed revision, with snapshot checks reading only committed HEAD,
host checks reading only host state, operational failures exiting 1
without a false verdict, and an actionable gap still exiting 0 with a
stable next action. No discrepancy between the stated intent and what
the tests actually prove.

### Pending rulings

(none)

### Report -- Pass 1

```
tests    11 renamed, 1 split, 0 added, 0 deleted
comments 421 -> 368   (in-scope files only; before = start of this pass)
structure 1 changes
suite    pass (matches baseline)    lint pass (matches baseline)
status   DONE — thorough
commits  e5fdda4 docs(codesignalcli): trim restated and issue-narrative comments in readiness contract
commits  9b7ddae test(coach): fix mismatched When precondition and trim narrative from specs
commits  eb0a10c docs(codesignalcli): strip issue-narrative citations from readiness diff comments
commits  dc2a5af refactor(coach): collapse duplicate check-project test env helper

kept comments (non-obvious only)
internal/codesignalcli/project_readiness.go (SupportedNodeMajor doc)              why not read mise.toml at runtime
internal/codesignalcli/project_readiness.go (fileExistsAtRevision doc)            git-plumbing 3-step protocol rationale
internal/codesignalcli/project_readiness.go (checkProjectShape doc)               untrusted-roots-until-policy-passes rationale
internal/codesignalcli/project_readiness.go (checkPolicy doc)                     git-read-failure-vs-content-rejection split
internal/codesignalcli/project_readiness.go (checkCompiler doc)                   forward-compat seam, not fabricating a result
internal/codesignalcli/project_readiness.go (CheckProjectReadiness ordering note) checkPolicy-before-checkProjectShape dependency trap
internal/codesignalcli/project_readiness.go (errNodeVersionProbeTimedOut doc)     why distinguished from errNodeNotFound
internal/codesignalcli/project_readiness.go (node probe timeout consts doc)       fail-closed subprocess-bound rationale
internal/codesignalcli/project_readiness.go (detectHostNodeMajor doc)             testability seam contract
internal/codesignalcli/project_readiness.go (checkNodeReadiness doc)              node_missing vs node_incompatible classification rule
internal/codesignalcli/project_readiness.go (dirty-worktree budget consts doc)    cross-file bound-consistency rationale
internal/codesignalcli/project_readiness.go (gitWorktreeStatus doc)               git status -z record-format protocol quirk
internal/codesignalcli/project.go (maxProjectConfigRoots doc)                     why the bound exists, cross-file cost reference
internal/codesignalcli/project.go (gitOperationalBoundErrorKind doc)              why bound-kind classification matters
internal/codesignalcli/project.go (parseProjectConfig doc)                        why it exists separately from validateProjectConfigJSON
internal/codesignalcli/project.go (loadProjectConfigForReadiness doc)             why its exit classification diverges from LoadProjectConfig's sibling path
internal/codesignalcli/project.go (hasDuplicatePaths comment, pre-existing)       why roots may nest but layer prefixes may not
internal/codesignalcli/project.go (forbidden_imports comment, pre-existing)       typo'd layer name would silently no-op
cmd/coach/main.go (validateCheckProjectFlags doc)                                 allowlist-by-construction rationale
cmd/coach/main.go (runCheckProject doc)                                          why exit 0 for an actionable gap
cmd/coach/project_readiness_acceptance_test.go (oversized-roots test comment)     why 257, and why timing alone wouldn't prove the bound

questions
internal/codesignalcli/project.go's parseProjectConfig (~90 lines: roots, layers, forbidden_imports validation) is pre-existing logic this PR only renamed and extended by one check; it would read more like prose split into validateRoots/validateLayers/validateForbiddenImports sub-functions, but that restructuring is orthogonal to this PR's own concern and touches security-relevant config-validation logic last substantively reviewed under a different PR. Recommend a dedicated follow-up pass, not folded in here.
```
