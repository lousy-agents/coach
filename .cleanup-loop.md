# Cleanup Loop State

## Pass 1 (PR #359)

### Note on prior content

This file carried Pass 1 records for already-merged PRs (#345, #347, #348).
Those diffs, baselines, and in-scope files are unrelated to PR #359
(`claude/awesome-lamport-r9op23`, guided TypeScript project-config authoring).
This run starts a fresh Pass 1 for #359 rather than resuming the wrong PR's
pass count.

### Baseline (pass-1, permanent reference, PR #359)

- `mise run gofmt`: exit 0, no files listed.
- `mise run go-vet` (`go vet ./...`): exit 0.
- `go test ./pkg/projectmodel/ ./internal/codesignalcli/ -count=1`: PASS.
- Focused `cmd/coach` authoring specs: PASS.
- Baseline failing tests: **none**. Baseline lint violations: **none**.

### In-scope files (PR #359 vs `origin/main` merge-base `e5bfa4a76b145309be7cee0086a80fe1ce3bfb65`)

Skipped for comment/SOLID: `.gitignore`, `go.mod`.

1. `cmd/coach/acceptance_test.go`
2. `cmd/coach/main.go`
3. `cmd/coach/main_test.go`
4. `cmd/coach/project_config_authoring_acceptance_test.go`
5. `cmd/coach/project_config_authoring_cli.go`
6. `internal/codesignalcli/controlling_terminal.go`
7. `internal/codesignalcli/controlling_terminal_other.go`
8. `internal/codesignalcli/controlling_terminal_pty_linux_test.go`
9. `internal/codesignalcli/controlling_terminal_pty_other_test.go`
10. `internal/codesignalcli/controlling_terminal_test.go`
11. `internal/codesignalcli/controlling_terminal_unix.go`
12. `internal/codesignalcli/controlling_terminal_unix_bsd.go`
13. `internal/codesignalcli/controlling_terminal_unix_tcgets.go`
14. `internal/codesignalcli/controlling_terminal_windows.go`
15. `internal/codesignalcli/project_config_authoring.go`
16. `internal/codesignalcli/project_config_authoring_test.go`
17. `internal/codesignalcli/project_config_suggestion.go`
18. `internal/codesignalcli/scope.go`
19. `pkg/projectmodel/ts_discovery.go`
20. `pkg/projectmodel/ts_workspace.go`
21. `pkg/projectmodel/ts_workspace_acceptance_test.go`

### PR intent (from PR body, restated from the tests)

Adds `--project-language typescript` to `--suggest-project-config`: discover
TypeScript roots from an immutable snapshot, then (only with a controlling
terminal) collect roots/layers/forbidden pairs/required layer, preview
coverage, and write a schema-1 project-config document on explicit approval.

### Pending rulings

None.

### Report — Pass 1

```
tests    21 renamed, 8 split, 4 added, 0 deleted
comments 1019 → 727   (in-scope files only; before = start of this pass)
structure 5 changes
suite    pass (matches baseline)    lint pass (matches baseline)
status   PASS 1/5
commits  eea6b9a test(codesignalcli): nest authoring specs so names carry the claims
commits  dc23ceb refactor(codesignalcli): split authoring collection from the approved write
commits  8c248a2 docs(codesignalcli): delete comments the code or tests already carry

kept comments (non-obvious only)
internal/codesignalcli/project_config_authoring.go:Roots          nil is not a discovered copy
internal/codesignalcli/project_config_authoring.go:Approved       decline is not cancellation
internal/codesignalcli/project_config_authoring.go:Cancelled      exhausted-input vs retry-or-cancel
internal/codesignalcli/project_config_authoring.go:readLine       any error is exhausted
internal/codesignalcli/project_config_authoring.go:parseRootSelection  out-of-range must not drop
internal/codesignalcli/project_config_authoring.go:directoryHasPrefix  must match layerContainsDir
pkg/projectmodel/ts_discovery.go:Stat                             never ReadFile before budget
cmd/coach/project_config_authoring_cli.go:rejectUnusableAuthoringOutput  O_EXCL remains authority
internal/codesignalcli/controlling_terminal.go                    ioctl not ModeCharDevice

questions
(none)
```

## Pass 1 (PR #362)

### Note on prior content

The section above is PR #359's completed Pass 1 (guided TypeScript
project-config authoring). This PR (#362, issue #326) is epic #280 Task 3
and starts its own Pass 1 rather than resuming #359's pass count.

### Baseline (pass-1, permanent reference for PR #362)

```
mise run gofmt                                          -> clean, no files listed
mise run go-vet                                          -> clean, no findings
go test -race ./cmd/coach/... -run Acceptance             -> ok  132.151s
go test ./internal/codesignalcli/...                      -> ok  13.459s
go test ./internal/validationtasks/...                    -> ok  0.024s
(js/semantics) npm run build:backend && npm run build && npm run build:test && node --test "dist-test/**/*.test.js"
                                                           -> tests 64, pass 64, fail 0
```

No linter beyond `gofmt`/`go vet` is configured for Go (no `.golangci.yml`);
no ESLint config or `lint` script exists for `js/semantics` (TypeScript
compilation via `tsc` is the closest static check and is included above via
`build`/`build:test`). Baseline is fully green — no pre-existing
failures/violations to exclude from this or later passes.

### In-scope files (PR #362 vs `origin/main` merge-base `e5bfa4a76b145309be7cee0086a80fe1ce3bfb65`)

In scope (rules applied):

1. `cmd/coach/no_findings_verdict_acceptance_test.go`
2. `cmd/coach/project_readiness_acceptance_test.go`
3. `cmd/coach/project_ts_backend_acceptance_test.go`
4. `internal/codesignalcli/project_acceptance_test.go`
5. `internal/codesignalcli/project_readiness.go`
6. `internal/codesignalcli/project_readiness_test.go`
7. `internal/codesignalcli/project_ts_backend.go`
8. `internal/codesignalcli/project_ts_compiler_resolve.go`
9. `internal/codesignalcli/project_ts_runtime.go`
10. `internal/codesignalcli/ts_analyzer_asset.go`
11. `internal/validationtasks/ts_project_backend_acceptance_wiring_acceptance_test.go`
12. `js/semantics/src/project-sidecar/analyze.ts`
13. `js/semantics/src/project-sidecar/edges-walk.ts`
14. `js/semantics/src/project-sidecar/edges.ts`
15. `js/semantics/src/project-sidecar/main.ts`
16. `js/semantics/src/project-sidecar/reachability.ts`
17. `js/semantics/src/project-sidecar/type-only.ts`
18. `js/semantics/src/project-sidecar/vfs.ts`
19. `js/semantics/test/project-sidecar-harness.ts`
20. `js/semantics/test/project-sidecar.test.ts`

Excluded (generated, never hand-edited — mirrored by `mise run
ts-analyzer-embed-sync` from the `js/semantics` build output, per "skip
... fully generated artifacts"):

- `internal/codesignalcli/tsanalyzerasset/project-sidecar/*.{js,js.map,d.ts,d.ts.map}` (26 files)

Excluded (deleted in this PR, nothing to clean up):

- `cmd/coach/testdata/fake_ts_sidecar/main.go`

Ruling required — pure config files where comment/SOLID rules don't
meaningfully apply (recorded, not acted on; same treatment as `mise.toml`
in #347's section above):

- `.github/workflows/ci.yml`
- `mise.toml`

### PR intent (from PR body, restated from the tests)

PR body (#362): resolves the customer's exact TypeScript compiler via a
frozen origin precedence (project manifest → project mise → global
mise) and flips `--project-language typescript` analysis from a
vendored, repository-relative sidecar lookup to Coach's embedded
analyzer plus that resolved compiler, spawned via the resolved Node
executable directly. From the tests alone: `project_readiness_acceptance_test.go`'s
new compiler-resolution specs (exact-project, project-mise-only,
global-mise-only, mismatch, conflict, unreadable-manifest, precedence
order) document the resolver's contract precisely; `project_ts_backend_acceptance_test.go`'s
real-compiler, no-vendored-sidecar specs and its native-package/unstable-API
failure-mode specs document the runtime flip and its qualified-incomplete
behavior; `project-sidecar.test.ts`'s `--compiler-module` specs document
the analyzer's explicit dynamic-load contract. No discrepancy between
the stated intent and what the tests actually prove.

### Pending rulings

1. `.github/workflows/ci.yml`, `mise.toml` — config files, comment/SOLID
   rules not meaningfully applicable. Recommendation: leave as-is
   (consistent with #347's section above).

### Report — Pass 1

```
tests    7 renamed, 0 split, 3 added, 0 deleted
comments 1245 → 932   (in-scope files only; before = start of this pass)
structure 9 changes
suite    pass (matches baseline)    lint pass (matches baseline)
status   PASS 1/5
commits  61efc60 refactor(codesignalcli): dedupe bounded-subprocess probes and origin lists
commits  238d042 docs(codesignalcli): trim restated and now-redundant comments
commits  56d25c2 test: fix a misleading When name and an overclaiming test comment
commits  ab6eee8 docs(coach): trim readiness spec comments already carried by names/asserts
commits  ac61170 test(coach): fold discriminating claims into It names
commits  328b368 docs(codesignalcli): trim restated comments, fold claims into It names
commits  fb02c1e refactor(semantics): rename visitedPaths to newlyVisitedPaths
commits  2445ac8 docs(semantics): trim restated comments in analyze/type-only/vfs
commits  d8a8ce9 docs(semantics): trim restated comments in reachability.ts
commits  ff1c7fc docs(semantics): trim duplicated/restated comments in main.ts
commits  e753d54 test(semantics): trim restated comments and dangling report references

kept comments (non-obvious only)
internal/codesignalcli/project_readiness.go (fileExistsAtRevision doc)          git-plumbing 3-step protocol rationale
internal/codesignalcli/project_readiness.go (CheckProjectReadiness ordering)    checkPolicy-before-checkProjectShape dependency trap
internal/codesignalcli/project_ts_compiler_resolve.go (locateMiseTypescriptInstall) why no cmd.Dir -- avoids re-admitting untrusted mise.toml hooks
internal/codesignalcli/project_ts_compiler_resolve.go (resolveMiseProjectCompiler) deliberately not a general TOML parse -- hazard scope limit
internal/codesignalcli/project_ts_runtime.go (PrepareTSRuntime)                 fatal-vs-soft-degrade contract; ownership/cleanup contract
internal/codesignalcli/ts_analyzer_asset.go (tsAnalyzerShimAssetPath)           embed.FS mode-0444 quirk
js/semantics/src/project-sidecar/main.ts (AST_REQUIRED_EXPORTS)                 untestable sync-with-call-sites invariant
js/semantics/src/project-sidecar/main.ts (describeErrorWithoutPaths)            Node error.message-always-embeds-a-path quirk
js/semantics/src/project-sidecar/vfs.ts (confinedFileSystem readFile)          null-vs-undefined snapshot-confinement contract
js/semantics/src/project-sidecar/analyze.ts (SidecarBackendError doc)          Go-side DiagBackendUnavailable parity requirement
js/semantics/src/project-sidecar/reachability.ts (sinkIdForDeclaration doc)    PrismaClient enclosing-class walk rationale

questions
1. cmd/coach/project_ts_backend_acceptance_test.go and internal/codesignalcli/project_acceptance_test.go duplicate an entire "install/locate the real installed TypeScript compiler" helper family (ensureRealTypeScriptCompilerAvailable, repoRootFromThisFile/tsAcceptanceRepoRootFromThisFile, npmArchName, realTypescriptVersion, copyFileTree, installRealTypescriptCompiler(At)), including their doc comments, verbatim across two packages. Genuine duplication, not superficial similarity -- worth extracting into a shared non-test support package (e.g. internal/tstestutil) importable by both. Deferred: this is a larger, cross-package refactor with its own risk profile, not done this pass.
2. js/semantics/src/project-sidecar/edges-walk.ts's edgeFromNode/edgeFromCall/buildEdge and reachability.ts's collectRouteRegistrations/processRouteRegistration/walkSourceForReachability/handleCallInSource/recordGapDiagnostic each thread the same 5-8-parameter cluster (project, snapshot, compiler/ast, plus 2-4 more) through unchanged. A collapsing context object would remove real duplication, but touches the hottest, most mutation-tested code paths in the PR -- deferred to a future pass rather than risked here.
3. js/semantics/test/project-sidecar.test.ts is a flat sequence of 27 node:test calls with scenario grouping expressed only through name prefixes ("--compiler-module:", "TS reachability:", etc.), not real describe() nesting. The single largest test ("resolved route-to-query facts, explicit coverage gaps, and a bad-tsconfig skip") bundles at least 4 independent claims, one of which (a malformed-tsconfig scenario using its own independent runSidecar call) costs nothing to extract into its own test. setupAlternateCompiler's 4 boolean/string flags, of which only one is ever set per call site, would read more clearly as 4 purpose-named factory functions. All deferred: this is the single largest remaining piece of work, touches assertion behavior in the most heavily mutation-tested file in the PR, and deserves its own careful pass rather than being rushed alongside everything else done here.
4. Two comment pairs were deliberately kept as lightweight cross-references rather than deleted, since the alternative (extracting a shared assertion helper, or splitting a test) is the same class of restructuring as item 3: project-sidecar.test.ts's two "-- see the 'resolved route-to-query facts' test above for why" pointers (lines ~793, ~842), and one "(see the 'invalid config' spec above)" pointer (~line 1021). Not a defect -- just flagging that a future pass extracting a shared helper would also let these become self-contained.
```

## Pass 1 (PR #372)

### Note on prior content

The sections above are Pass 1 for already-merged PRs (#359, #362). This PR
(#372, `opencode/d2-handoff-1-326`, Progresses #326 Handoff 1) starts its
own Pass 1 rather than resuming those pass counts.

### Baseline (pass-1, permanent reference for PR #372)

```
mise run gofmt                    -> exit 0
mise run go-vet                   -> exit 0
mise run ci-fast                  -> exit 0 (cmd/coach 140.202s, all packages ok)
```

Baseline failing tests: **none**. Baseline lint violations: **none**.

### In-scope files (PR #372 vs `origin/main` merge-base `fbb00ed5f3b429980c4282df1db438d38a388a6e`)

Skipped for comment/SOLID (generated embed): `internal/codesignalcli/tsanalyzerasset/**`.
Skipped: `docs/architecture/system-overview.md` (architecture prose; D-09 sentence is the required edit).

### PR intent (from PR body, restated from the tests)

Coach resolves a version-equal native TypeScript package next to the compiler,
requires `--native-package`, sets `tsserverPath` to `lib/tsc`, returns empty
VFS listings on miss, probes `process.execPath`, sets child PATH to the runtime
directory, and refuses Node majors outside {24, 26} with `node_missing`.

### Pending rulings

1. Duplicate real-TypeScript-compiler test helpers across cmd/coach and
   internal/codesignalcli (carried from #362). Recommendation: extract later.
2. Remaining flat `node:test` prefixes in project-sidecar.test.ts outside the
   native-package/VFS/constructor groups nested this pass.

### Report — Pass 1

```
tests    1 renamed, 1 split, 1 added, 0 deleted
comments 896 → 875   (in-scope files only; before = start of this pass)
structure 0 changes
suite    pass (matches baseline)    lint pass (matches baseline)
status   PASS 1/5
commits  407c7bb test(semantics): nest native-package and VFS listing specs
commits  4c86274 docs: drop restated runtime and VFS comments the tests already pin

kept comments (non-obvious only)
js/semantics/src/project-sidecar/vfs.ts (readFile miss)           null-vs-undefined fall-through
js/semantics/src/project-sidecar/vfs.ts (fileExists not wrapped)  wrapping fileExists is not AC-RUN-3
internal/codesignalcli/project_ts_runtime.go (PrepareTSRuntime)   fatal-vs-soft-degrade contract
internal/codesignalcli/project_ts_runtime.go (resolveHostNode)    readiness probe vs spawn path
pkg/projectmodel/ts_sidecar.go (BinaryPath)                       not a PATH lookup
pkg/projectmodel/ts_sidecar_io.go (sanitizedTSSidecarEnv)         AC-RUN-2/4 PATH-only env

questions
Duplicate compiler-install test helpers across cmd/coach and internal/codesignalcli. Recommendation: extract to a shared test support package in a later pass.
Remaining project-sidecar.test.ts suites still use name prefixes instead of describe(). Recommendation: nest in a later pass; this pass nested only the Handoff 1 specs.
```

## Pass 1 (PR #373)

### Note on prior content

The sections above are Pass 1 for already-merged PRs (#359, #362, #372). This PR
(#373, `opencode/d2-linux-close-326`, Progresses #326 Handoff 2) starts its
own Pass 1 rather than resuming those pass counts.

### Baseline (pass-1, permanent reference for PR #373)

```
mise run gofmt                    -> exit 0
mise run go-vet                   -> exit 0
go test ./cmd/coach/... -run Acceptance -ginkgo.focus="file-syscall trace parser" -> ok
go test ./internal/validationtasks/... -run Acceptance -> ok
```

Linux file-syscall and unshare specs Skip on darwin (D-LNX-08). Baseline
failing tests: **none**. Baseline lint violations: **none**.

### In-scope files (PR #373 vs `origin/main` merge-base `8d509cb04df2791a362806b265ce5eac7b12ab4b`)

1. `cmd/coach/project_ts_backend_acceptance_test.go` (large; changed region + local problems)
2. `cmd/coach/project_ts_foreign_clone_acceptance_test.go`
3. `cmd/coach/project_ts_linux_confinement_acceptance_test.go`
4. `cmd/coach/project_ts_linux_confinement_trace_test.go`
5. `internal/validationtasks/ts_project_backend_acceptance_wiring_acceptance_test.go`

Skipped for comment/SOLID: `docs/architecture/system-overview.md`,
`docs/product/evaluations/codesignal-pilot-readiness.html`.

### PR intent (from PR body, restated from the tests)

Linux `ts-project-backend` file-syscall traces keep the confined analyzer
subtree inside a frozen allowlist and record a typeRoots decoy only on the
unconfined throwaway; unshare yields the same complete report as a
non-unshare baseline.

### Pending rulings

1. `.github/workflows/ci.yml`, `mise.toml` — config files. Recommendation: leave as-is.
2. `docs/architecture/system-overview.md`, `docs/product/evaluations/codesignal-pilot-readiness.html` — architecture/eval prose. Recommendation: leave as-is.

### Report — Pass 1

```
    tests    4 renamed, 2 split, 0 added, 1 deleted
    comments 34 → 24   (in-scope files only; before = start of this pass)
    structure 3 changes
    suite    pass (matches baseline)    lint pass (matches baseline)
    status   PASS 1/5
    commits  1e42c0c test: nest Linux confinement parser and wiring claims
    commits  d7d2e75 refactor(test): drop unused strace PID exclusion and allowlist alias
    commits  1d73b57 docs: drop restated Linux confinement comments the tests already pin

    kept comments (non-obvious only)
    cmd/coach/project_ts_linux_confinement_trace_test.go:17  D-LNX-01 %file trap

    questions
    Remaining pre-existing helper comments in cmd/coach/project_ts_backend_acceptance_test.go outside the Handoff 2 region. Recommendation: leave; prior PRs already cleaned them.
```
